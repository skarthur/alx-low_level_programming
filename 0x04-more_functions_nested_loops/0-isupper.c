#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: character input
 * Return: 1 if c is uppercase, 0 if not
 */

int _isupper(int c)
{
	int i;

	if (i >= 'A' && i <= 'Z')
		return (1);
	else
		return (0);

	_putchar('\n');
}
